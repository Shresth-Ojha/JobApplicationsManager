services:
  postgres:
    image: postgres:15-alpine
    container_name: jam_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: jam
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d jam" ]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  adminer:
    image: adminer
    container_name: jam_adminer
    ports:
      - "8080:8080"
    networks:
      - app-network
    depends_on:
      - postgres
    restart: always

  backend:
    build: ./server
    image: jam-backend:latest
    container_name: jam_backend
    environment:
      DATABASE_URL: postgresql://user:password@postgres:5432/jam?schema=public
      JWT_SECRET: supersecretjwtkey
      JWT_REFRESH_SECRET: supersecretrefreshjwtkey
    command: sh -c "npx prisma generate && npx prisma migrate deploy && npm run dev"
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
    volumes:
      - ./server:/app
      - /app/node_modules

  frontend:
    build: ./client
    image: jam-frontend:latest
    container_name: jam_frontend
    environment:
      VITE_API_URL: http://localhost:3000/api/v1
    command: sh -c "npm install && npm run dev"
    ports:
      - "5555:5555"
    depends_on:
      - backend
    networks:
      - app-network
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
    volumes:
      - ./client:/app
      - /app/node_modules

volumes:
  postgres_data:


networks:
  app-network:
    driver: bridge
